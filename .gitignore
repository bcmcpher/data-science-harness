# Local source material (copyrighted / large reference PDFs) — kept out of the repo
/resources/

# MyST build output
paper/_build/

# Tooling environments — see pyproject.toml, package.json, environment.yml
node_modules/
.venv/
__pycache__/
*.egg-info/
