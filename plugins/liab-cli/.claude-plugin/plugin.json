{
  "name": "liab-cli",
  "description": "Lab-in-a-Box skill toolbox: pyinfra deployment planning and (on an explicit target-naming instruction) application, plus the offline check the liab doer runs first. Plan-by-default is a constraint rather than a preference here, because the blast radius is a machine rather than a working tree — and a green pyinfra run means the operations applied, not that the service serves.",
  "version": "0.1.0",
  "author": { "name": "bcmcpher" },
  "license": "MIT",
  "keywords": ["pyinfra", "lab-in-a-box", "self-hosting", "forgejo", "git-annex", "data-sovereignty", "distributability", "toolbox", "capability"],
  "skills": [
    "./skills/pyinfra"
  ]
}
